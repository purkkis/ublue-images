---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: dnf
    install:
      packages:
        - bubblewrap
    group-install:
      packages:
        - development-tools
      with-optional: false
  - type: brew
    nofile-limits: true
    auto-upgrade: false
    auto-update: false
