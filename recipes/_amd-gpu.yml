---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    install:
      packages:
        - linux-firmware
        - amd-gpu-firmware
        - mesa-vulkan-drivers
        - mesa-dri-drivers
        - mesa-libGL
